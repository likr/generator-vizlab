language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-vizlab' ]; then cd .. && eval "mv $currentfolder generator-vizlab" && cd generator-vizlab; fi

